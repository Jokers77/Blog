<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Simple Blog</a>
    </div>
    <div>
      <ul class="nav navbar-nav">
        <li class="<%= yield(:home) %>" ><%= link_to "Home", root_path %></li>
        <li class="<%= yield(:contact) %>" ><%= link_to "Contact", contact_path %></li>
      </ul>
      <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.try(:admin?) %>
        <li><%= link_to "Gestions des articles", "posts" %></li>
        <% end %>
        <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
      <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= yield(:sign_up) %>"><a href="/users/sign_up"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
        <li class="<%= yield(:sign_in) %>"><a href="/users/sign_in"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>